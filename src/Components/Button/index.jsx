import React from "react";
import { StyledButton } from "./button-styled";


function Button() {
    return (
        <StyledButton>
        <div>
        </div>
        </StyledButton>

    );
  }
  
  export default Button;